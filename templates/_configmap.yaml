{{range $key, $e := .Values.applications.name }}
{{range $keys, $env := $.Values.environment.name }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $e }}-{{ $env }}
  list: {{ $env }}-{{ $e }}
  cpu: |
      - {{ $env }}
data:
  myvalue: "Hello World"
{{ end }}
{{ end }}